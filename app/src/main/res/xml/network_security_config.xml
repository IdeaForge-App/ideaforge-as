<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for IdeaForge.
    - Cleartext (HTTP) is BLOCKED globally.
    - Exceptions are granted ONLY to the Android emulator loopback
      (10.0.2.2) and localhost for local Spring Boot development.
    - In production builds, all traffic MUST go over HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext only for local development endpoints -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
